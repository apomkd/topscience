name: ci
on:
  pull_request:
    branches: [develop, main]
  push:
    branches: [develop]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "lint placeholder"
      - run: echo "typecheck placeholder"
      - run: echo "test placeholder"
      - run: echo "build placeholder"
      - run: bash ops/ci/gates.sh
